//Список всех добавленных привычек с указанием статуса выполнения (например, галочка).
// Кнопка добавления новой привычки.
// Возможность фильтрации списка по статусу (выполнено/не выполнено).

const MainScreen = () => {
  return(<>
    <h1>Main Screen</h1>
  </>);
}

export default MainScreen;
